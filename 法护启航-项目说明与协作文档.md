# 法护启航 · 项目说明与协作文档

> 面向大学生的 AI 法律安全驾驶舱（前端原型）
> 副标题：让每一次签约，都多一份底气
> 文档更新：2026-09-09 · 版本：v1.2（功能演示版）

---

## 0. 一句话定位

「法护启航」是一个**单文件、零依赖、可双击运行**的 Web 前端原型，覆盖大学生高频法律风险（租房 / 兼职就业 / 合同纠纷），包含 5 个页面 + 底部固定导航。所有数据与 AI 回答均为**前端模拟演示**，用于产品演示、答辩、以及后续接真实数据的 UI/交互基底。

---

## 1. 仓库 / 文件清单

| 文件 | 说明 |
|---|---|
| `index.html` | 全站唯一运行文件：HTML + CSS + JS 全部内嵌（约 108 KB），浏览器直接打开 |
| `法护启航-项目说明与协作文档.md` | 本文档 |
| `法护启航_答辩PPT.pptx` | 答辩演示文稿（配套） |
| `build_ppt.py` | PPT 生成脚本（如需改稿可编辑后重跑） |
| `assets/` | PPT 相关素材 |

> 运行方式：双击 `index.html`，或拖入 Chrome / Edge 打开。无需安装、无需服务器、无网络依赖。

---

## 2. 页面结构与功能清单

底部固定导航 5 项：`首页` `AI助手` `合同审查` `风险地图` `我的权益`，页面切换为纯 JS 模拟（`showPage()`），无路由、无后端。

### 2.1 首页（page-home）
- Hero 品牌区：法护启航 + 副标题 + 功能关键词。
- 核心搜索区「🔍 我遇到什么问题？」：输入框占位「房东不退押金怎么办？」，热门问题标签一键提问。
- 三个快捷入口卡片：🏠 租房安全 / 💼 兼职就业 / 📄 AI 合同审查（点击跳转对应功能并自动带入问题）。
- **30 秒法律风险测试**：4 道选择题 → 打分 → 输出 🟢/🟡/🔴 风险等级与建议（可重测、可转 AI）。

### 2.2 AI 助手（page-ai）
- 非普通聊天框：先展示 6 个场景入口（房东扣押金 / 兼职欠薪 / 合同问题 / 试用期辞退 / 虚假招聘 / 其他）。
- 点击进入模拟对话：机器人「正在输入」动画 → 结构化回答（分步骤 + 证据清单 + 法律依据行）。
- 支持快捷追问 chip、自由输入（关键词自动匹配场景或走通用回答）、返回问题列表。
- 数据位置：JS 对象 `SCEN`（含每个场景的 `ask / ans / su` 追问）。

### 2.3 合同审查（page-review）— 答辩核心爆点页
- 「AI 合同风险雷达」四步流程：**上传 → 扫描动画 → 风险报告 → 操作**。
- 上传：拖拽 / 点击选择，支持 PDF / Word / 图片（演示仅校验扩展名）。
- 扫描：雷达旋转动画 + 5 阶段进度条（解析→切分→语义分析→法条比对→生成报告）。
- 报告：**综合风险得分 72/100**（圆环动画），统计 🔴高风险 3｜🟡中风险 5｜🟢低风险 12（共 20 处，与列表一致）。
- 每条风险卡：风险条款标题 + **风险原因 / 修改建议 / 法律依据**，可展开折叠、可按严重度筛选。
- 数据位置：`RISK`（高/中风险条目）、`LOW_TIPS`（低风险 12 条）。

### 2.4 风险地图（page-map）— 区域化数据驾驶舱
- **背景数据：昆明呈贡大学城，以云南大学为中心片区**（6 个片区，合计 1,335 起模拟纠纷）。
- 顶部指标条：收录总数 / 高发类型 / 覆盖片区 / 周风险指数（与数据联动计算）。
- 地图：装饰底图（道路 / 水域 / 绿地 / 街道名：吴家营街道、雨花街道、斗南片区、昆明南站）+ 6 个热点。
- 交互：类型筛选（租房 / 兼职就业 / 消费培训 / 人身其他）、🔥 热力图层开关、⟳ 模拟刷新（带“刚刚更新”时间戳）、热点点击钻取右侧详情。
- 右侧详情面板：总览（类型分布条 + 高发片区 Top3）与片区详情（风险等级、环比、类型构成、脱敏典型案例、维权指引、咨询 AI / 附近法援点）。
- 数据位置：`MAP_DATA`（片区数据）、`TYPE_META`（类型元数据：名称 / 图标 / 颜色）。

### 2.5 我的权益（page-rights）
- 身份卡：应届毕业生、档案完整度 68%、毕业城市（昆明）。
- 风险标签：🔴 就业合同 / 🟡 租房 / 🟢 兼职（点击跳转处理）。
- 证据材料：租房合同 / 房屋照片 / 聊天记录 / 工资记录（已存证）+ 「上传新证据」模拟添加。
- 法援通道：12348 热线、高校法律援助中心（模拟 Toast）。

---

## 3. 技术架构速览（改哪里看这里）

单文件内部分三大段，均有中文注释：

### 3.1 CSS 关键变量（改风格先改这里）
`:root` 中定义了全套设计令牌：主色 `--brand:#2f6bff`、深蓝 `--brand-deep:#1e3a8a`、底色 `--bg:#e9eef6`、风险色 `--high/--mid/--low`、圆角与阴影。全站主渐变 `--grad`。

### 3.2 页面 HTML
5 个 `<section class="page" id="page-...">` + 底部 `<nav class="bottomnav">`。加页面 = 复制一个 section + 在导航加按钮 + 在 `showPage` 无需改动（按 id 切换）。

### 3.3 核心 JS 函数与数据对象
| 名称 | 作用 |
|---|---|
| `showPage(id)` | 页面切换（含回到 AI 列表逻辑） |
| `toast(msg, ok)` | 全局轻提示 |
| `SCEN` | AI 6 个场景的模拟对话内容 |
| `detectScene(text)` / `askAI(text)` | 关键词匹配场景 / 带问题跳 AI |
| `QUIZ` | 首页风险测评题目 |
| `RISK` / `LOW_TIPS` | 合同审查报告数据 |
| `MAP_DATA` / `TYPE_META` | 风险地图数据源 |
| `mapState` | 地图交互状态（类型筛选 / 热力 / 选中片区） |
| `renderMapAll()` | 地图整页重渲染入口 |

> 接真实数据的最小改动：**只替换 `MAP_DATA` / `RISK` / `SCEN` 等数据对象，逻辑代码基本不动**。

---

## 4. 设计规范（保持年轻 + 专业 + 可信）

- **配色**：蓝灰科技风。深蓝 `#0d2557` → 亮蓝 `#2563eb` → 青 `#0ea5e9` 渐变主视觉；背景浅灰蓝 + 细网格 + 光晕；白色卡片。
- **风险语义色**：红=高风险 / 黄=中风险 / 绿=低风险，全站统一。
- **字体**：系统字体栈（PingFang SC / 微软雅黑），无外部字体依赖（可离线）。
- **形状**：圆角卡片（16–22px）、大阴影、毛玻璃顶栏与底部导航、柔和微动画。
- **图标**：emoji（🏠💼📄🗺️👤）降低实现成本、贴合年轻调性；答辩如需更商务可换 SVG。
- **不要**：政务蓝白红、国徽印章、密集表格政府风；内容可严肃，视觉要现代。

---

## 5. 免责与合规声明（务必保留）

- 全站为**演示 / 模拟**，AI 回答与数据不构成法律意见。
- 各页页脚/说明处已内置「不构成法律意见，紧急拨打 12348」提示，正式发布前请由法律专业人士复核并调整文案。
- 若后续接入真实用户数据，需补充隐私政策与数据脱敏。

---

## 6. 多人协作约定（供团队参考）

1. **一个入口**：改动集中在 `index.html`；复杂数据可先抽到文件顶部便于评审。
2. **加数据 ≠ 改逻辑**：新增片区 / 场景 / 风险条目只改对应数据对象；新增交互再动函数。
3. **命名**：JS 对象用大写（数据）与小驼峰（函数）区分；id/class 语义化中文注释。
4. **不要引入外部依赖**：原型坚持原生三件套，确保任何浏览器双击即用、答辩不断网。
5. **提交前自检**：改完 JS 后做一次语法检查（见第 7 节），并跑一遍 5 页演示动线。
6. **分工建议**：一人管数据内容（法条 / 案例 / 文案），一人管交互与视觉，一人管答辩 PPT；以本文档 + `index.html` 注释为唯一事实源。

---

## 7. 快速自检清单

- [ ] 底部 5 个导航均能切换且无报错
- [ ] 首页：搜索可跳 AI；测评可出结果
- [ ] AI：6 场景入口 → 对话 → 追问 → 返回列表
- [ ] 合同审查：选文件 → 扫描动画 → 72 分报告 → 筛选/展开/导出(Toast)
- [ ] 风险地图：筛选类型 → 点击热点出详情 → 热力开关 → 模拟刷新
- [ ] 我的权益：上传证据新增卡片；12348 Toast
- [ ] 无“杭州”残留（背景已换昆明呈贡 / 云南大学）
- [ ] JS 语法检查通过（Node：抽取 `<script>` 内容执行 `new vm.Script(code)`）

---

## 8. 待办 / 路线图（含你关心的「具体法条回复」）

### 8.1 需求：AI 咨询回复中给出「对应的具体法律条文」——可以实现 ✅

**结论：可以做到，且建议在演示版用「确定性法条库」而非让大模型自由发挥。**

现状：AI 回答尾部已带「📌 参考依据」行（如《民法典》第 585 条），但只是**条号**，不是**条文原文**，自由输入时也未必命中。

推荐方案 A（演示版，推荐，可离线、零幻觉）：
- 新增 JS 数据对象 `LAW_DB`：按场景映射到「法律名称 + 条号 + 条文原文 + 来源」，原文**人工录入 / 官方核对**；
- AI 回复末尾自动追加「⚖️ 法律依据」卡片，引用 `LAW_DB[场景].条号 + 条文摘录 + 来源链接（演示可不链）`；
- 自由输入时先用 `detectScene()` 命中场景，再自动带出对应法条；命中不了则明确说「请核对以下方向」而不是编造条号。
- 优点：可校验、可审计、答辩时可逐条展示依据来源；与「维权 Agent」类项目的可信做法一致。

方案 B（联网增强，后续可做）：
- 接入法律大模型 / RAG（需 API Key + 后端 + 法条语料库），回答带引用；
- 必须增加「引用校验门禁」防条号幻觉，复杂度与成本高，演示环境不建议。

落地建议：先做方案 A，把 `SCEN` 每个场景配 2–4 条真实法条（含原文），并在聊天气泡中渲染成可折叠的「法律依据」卡片。**如需要，下一步可直接在本原型中实现。**

### 8.2 其他待办
- [ ] 法条库 `LAW_DB` + 聊天「法律依据」卡片渲染（8.1）
- [ ] 风险地图接入真实数据源（替换 `MAP_DATA`，或接后端接口）
- [ ] 合同审查支持多份文件、报告导出为真实文件
- [ ] 证据材料页增加「到期提醒 / 时效倒计时」
- [ ] 部署到 GitHub Pages 生成可分享链接
- [ ] 精简版代码（降低二次开发阅读成本）

---

*文档结束 · 欢迎团队在 Issue / 群内基于本文档分工协作*