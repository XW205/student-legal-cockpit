# 法护启航 · 项目说明与协作文档

> 面向大学生的 AI 法律安全驾驶舱（前端原型）
> 副标题：让每一次签约，都多一份底气
> 文档更新：2026-09-09 · 版本：v2.2（七页界面重构 · 移动端底部导航）

---

## 0. 一句话定位

「法护启航」是一个**原生 HTML/CSS/JS、零构建、可双击运行**的 Web 前端原型，覆盖大学生高频法律风险（租房 / 兼职就业 / 合同纠纷），包含 7 个页面（首页 / AI风险测评 / AI合同审查 / 维权导航 / 线下法律服务地图 / 证据急救 / 关于我们），桌面端顶部导航、移动端（≤960px）底部 Tab 导航。代码按结构 / 样式 / 逻辑拆分为 `index.html`、`styles.css`、`app.js` 三个文件，并已部署 GitHub Pages。

AI 能力为**双通道、双场景**设计：

- **🧪 离线演示模式（默认）**：AI 助手内置场景库 + 法条库，合同审查内置示例报告，零网络可答辩；
- **🎓 DeepSeek 云端模型模式**：在 AI 助手页 ⚙️ 设置填入 DeepSeek API Key 后，**AI 助手**（任意法律问答）与**AI 合同风险雷达**（合同文本 → 结构化 JSON 风险报告）均由真实大模型驱动；请求失败时 AI 助手自动回退离线回答、合同审查进入失败页并可查看离线演示报告。

此外，「我的权益」页已落地**本机真实档案**：注册 / 登录（localStorage）、证据材料真实上传存证、证据链一键汇总与导出；风险地图等其余页面数据仍为前端模拟，作为后续接真实数据的交互基底。

---

## 1. 仓库 / 文件清单

| 文件 | 说明 |
|---|---|
| `index.html` | 页面结构：7 个 `<section class="page">` + 桌面顶部导航 / 移动端底部 Tab 与「全部功能」弹层 + AI 设置弹窗 + Toast；`<head>` 引 `styles.css` 与 marked CDN， body 末尾引 `app.js` |
| `styles.css` | 全站样式：`:root` 设计令牌 + 7 页全部组件样式 + UI 动效 + 响应式（UTF-8 无 BOM） |
| `app.js` | 全站逻辑：数据对象 + 交互 + DeepSeek 接入 + 浏览器本地文件解析（UTF-8 无 BOM；由原 head marked 配置与 body 主脚本两段合并） |
| `README.md` | 项目门面文档：简介、快速开始、功能导览 |
| `README-AI接入.md` | AI 大模型接入说明（AI 助手 + AI 合同审查双场景、文件解析、排障） |
| `法护启航-项目说明与协作文档.md` | 本文档：架构、数据、协作约定 |
| `UI设计说明.md` | UI 设计说明：配色 / 字体 / 排版规范、页面交互逻辑标注、UI 动效规范、MCP/Skill 插件加载方式 |
| `.gitignore` | Git 忽略规则（Office 临时锁文件、Python 缓存、系统文件） |

> 运行方式：双击 `index.html`，或拖入 Chrome / Edge 打开；也可访问在线版 <https://xw205.github.io/student-legal-cockpit/>（GitHub Pages）。
> **离线演示模式**下无需安装、无需服务器、无网络依赖；DeepSeek 云端模式需要网络与 API Key，文件解析库首次使用需从 CDN 加载（详见 `README-AI接入.md`）。
> 注：答辩 PPT 等配套材料不在本仓库内，另行提供。

---

## 2. 页面结构与功能清单

桌面端顶部导航 7 项；移动端（≤960px）隐藏顶栏，改为**底部 Tab 导航**（`首页` `AI测评` `AI审查` `咨询` `更多`），点「更多」展开“全部功能”弹层（`.sheet`）。页面切换为纯 JS 模拟（`showPage()`），无路由、无后端。
> 页面 id → 导航名速查：`page-home` 首页 · `page-quiz` AI 风险测评（测评由首页迁至独立页）· `page-review` AI 合同审查 · `page-ai` 维权导航（原“AI 助手”）· `page-map` 线下法律服务地图（原“风险地图”）· `page-rights` 证据急救（原“我的权益”）· `page-about` 关于我们（新增，纯静态）。

### 2.1 首页（page-home）
- Hero 品牌区：法护启航 + 副标题 + 功能关键词。
- 核心搜索区「🔍 我遇到什么问题？」：输入框占位「房东不退押金怎么办？」，热门问题标签一键提问。
- 三个快捷入口卡片：🏠 租房安全 / 💼 兼职就业 / 📄 AI 合同审查（点击跳转对应功能并自动带入问题）。
- **法律风险测试（v2.0 升级为 20 题）**：5 大领域各 4 题（🏠 租房 / 💼 兼职就业 / 📄 合同消费 / 🔐 网络资金 / 🛡️ 纠纷维权）→ 按得分率输出 🟢/🟡/🔴 风险等级与建议，并给出**分领域风险明细 chips**（可重测、可转 AI）。

### 2.2 维权导航（page-ai）— 双通道：离线演示 + DeepSeek 云端大模型
- 非普通聊天框：先展示 6 个场景入口（房东扣押金 / 兼职欠薪 / 合同问题 / 试用期辞退 / 虚假招聘 / 其他）。
- 点击进入对话：机器人「正在输入」动画 → 结构化回答（分步骤 + 证据清单 + 法律依据行）。
- 支持快捷追问 chip、自由输入、返回问题列表、清空对话（清空同时中断进行中的请求）。
- **Markdown 渲染**：气泡内容经 `marked` 解析（CDN），加载失败自动回退纯文本。
- **📖 法律依据卡片（v2.0 新增）**：回答文本中出现《民法典》第 x 条等引用时，自动匹配内置法条库 `LAW_DB`（5 部法律、30 条原文），在气泡末尾追加可折叠卡片展示条文原文与核对提示。
- **🧪 离线演示模式（默认）**：6 大场景标准答案（`SCEN`）+ 7 类关键词分类兜底（`offlineAnswer`：消费退费 / 校园贷 / 人身侵权 / 网络侵权 / 知识产权 / 房屋维修 / 劳动权益）+ 通用维权建议，零网络、零成本。
- **🎓 DeepSeek 云端模型模式**：⚙️ 设置弹窗填 Key 后启用。浏览器直连 OpenAI 兼容接口（默认 `https://api.deepseek.com/v1/chat/completions`，模型 `deepseek-chat`），发送「法小航」系统提示 + 最近 12 轮对话（`temperature 0.7 / top_p 0.9 / max_tokens 2048 / 120s 超时`）；新提问中断旧请求；`content` 为空时回退读 `reasoning_content`（兼容 reasoner）；失败时错误气泡 + 独立离线回答气泡。
- 设置弹窗：模式切换、服务地址 / 模型名称 / API Key、🔌 测试连接（`GET /v1/models`，9s 超时）、💾 保存；配置存 localStorage（键 `deepseek_cfg_v1`）。
- 顶部状态点与对话标签实时显示当前模式。
- 详细接入步骤见 `README-AI接入.md`。

### 2.3 合同审查（page-review）— v2.0 升级为真实 AI 审查（答辩核心爆点页）
- 「AI 合同风险雷达」四步流程：**上传 / 粘贴 → 扫描动画 → 风险报告 → 操作**；失败时有独立「😥 AI 审查失败」阶段。
- **两种输入方式**：
  - 上传文件：拖拽 / 点击选择，支持 PDF / DOCX / 图片（JPG/PNG/WEBP）/ TXT / MD（≤20MB，仅校验扩展名与大小）；
  - 粘贴合同全文：文本域 +「📋 填入示例租房合同」一键填充（`SAMPLE_CONTRACT`）。
- **浏览器本地文字抽取**（文件不上传，解析库按需从 CDN 加载）：
  - PDF → `pdf.js` 抽取文字层（扫描件无文字层会提示改走图片 OCR / 粘贴）；
  - DOCX → `mammoth` 抽取纯文本（旧版 .doc 不支持）；
  - 图片 → `Tesseract.js` OCR（`chi_sim+eng`，首次下载中文模型，带进度）；
  - TXT/MD → `FileReader` 直读；解析 90s 超时，文本统一截断至 12,000 字。
- **DeepSeek 审查**：合同审查专用系统提示 + `response_format: { type: 'json_object' }`（`temperature 0.2 / max_tokens 12000 / 150s 超时`，空输出重试 1 次）；返回 JSON 经 `parseContractReport()` 解析：`score`（0–100，越高越危险）、`level`、`summary`、高/中/低计数、`risks[]`（条款位置 / 标题 / 风险原因 / 修改建议 / 法律依据，高中风险 ≤8 条）、`low_tips[]`（≤6 条）。
- **报告渲染**：综合得分圆环动画（≥70 红 / ≥45 黄 / 余绿）+ 签约建议、高/中/低统计 chips（与筛选按钮计数联动）、风险卡可展开折叠、可按严重度筛选、低风险折叠块、导出 TXT。
- **失败兜底**：未配置 / 未启用 DeepSeek、网络错误、超时、JSON 解析失败 → 失败页展示具体原因，提供「↻ 重新审查 / ✏️ 返回修改文本 / 🧪 查看离线演示报告」。
- **离线演示报告**：内置示例租房合同报告（72/100，🔴3 / 🟡5 / 🟢12），数据为 `RISK`（8 条高中风险）+ `LOW_TIPS`（12 条），仅展示效果。
- 粘贴区下方状态行 `#dsStatusLine` 实时显示 DeepSeek 连接状态（🟢 已连接 / 🟡 已填 Key 未启用 / ⚪ 未配置），后两者带快捷跳转配置按钮。

### 2.4 线下法律服务地图（page-map）— 区域化数据驾驶舱
- **背景数据：昆明呈贡大学城，以云南大学为中心片区**（6 个片区，`MAP_DATA` 合计约 1,335 起模拟纠纷，界面数字由数据实时求和）。
- 顶部指标条：收录总数 / 高发类型 / 覆盖片区 / 周风险指数（与数据联动计算）。
- 地图：装饰底图（道路 / 水域 / 绿地 / 街道名：吴家营街道、雨花街道、斗南片区、昆明南站）+ 6 个热点。
- 交互：类型筛选（租房 / 兼职就业 / 消费培训 / 人身其他）、🔥 热力图层开关、⟳ 模拟刷新（带“刚刚更新”时间戳）、热点点击钻取右侧详情。
- 右侧详情面板：总览（类型分布条 + 高发片区 Top3）与片区详情（风险等级、环比、类型构成、脱敏典型案例、维权指引、咨询 AI / 附近法援点）。
- 数据位置：`MAP_DATA`（片区数据）、`TYPE_META`（类型元数据：名称 / 图标 / 颜色）。

### 2.5 证据急救（page-rights）— v2.0 升级为本机真实档案
- **真实信息登录（v2.0 新增）**：首次录入（真实姓名 / 高校 / 学号 / 身份 / 密码 ≥6 位，两次确认）或学号 + 密码登录；单浏览器一份档案（`fahq_profile_v1`），登录态存 `fahq_session_v1`；密码经 `rlHash()` 简单哈希存储（**演示级，非加密**），可退出登录。
- 身份卡：姓名 / 高校 / 学号 / 身份标签、档案完整度 68%。
- 风险标签：🔴 就业合同 / 🟡 租房 / 🟢 兼职（点击跳转处理）。
- **证据材料真实存证（v2.0 升级）**：选择文件上传（PDF / 图片 / 音视频 / 压缩包等，转 dataURL 存 localStorage 键 `fahq_evidence_v1`），选择分类（合同 / 聊天记录 / 转账工资 / 照片视频 / 录音 / 其他）+ 备注后保存；单份 ≤2MB、总量约 ≤4MB；支持查看、删除、计数与空状态。
- **证据链汇总（v2.0 新增）**：按保存时间生成时间线，对照 6 类关键材料（`CHAIN_KEYS`：合同 / 聊天 / 转账 / 照片视频 / 录音 / 其他）做覆盖检查，输出完整度百分比、叙述性证据链文本、缺失材料提示，并可导出 `法护启航-证据链汇总.txt`。
- 法援通道：12348 热线、高校法律援助中心（模拟 Toast）。

---

## 3. 技术架构速览（改哪里看这里）

项目拆为三个文件，均有中文注释：

### 3.1 样式（styles.css）
`:root` 中定义全套设计令牌：主色 `--brand:#2f6bff`、深蓝 `--brand-deep:#1e3a8a`、底色 `--bg:#e9eef6`、风险色 `--high/--mid/--low`、圆角与阴影，全站主渐变 `--grad`。改风格先改这里。

### 3.2 页面结构（index.html）
7 个 `<section class="page" id="page-...">` + 桌面顶部 `<nav class="topnav">` / 移动端底部 `<nav class="mbottom">`（≤960px）与「全部功能」弹层 `.sheet`

```html
<head>
  <link rel="stylesheet" href="./styles.css">
  <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>   <!-- 必须在 app.js 之前 -->
</head>
<body>
  …页面结构…
  <script src="./app.js"></script>   <!-- body 末尾 -->
</body>
```

加页面 = 复制一个 section + 在导航加按钮（`showPage` 按 id 切换，无需改逻辑）。**GitHub Pages 等 Linux 环境必须用相对路径且文件名大小写一致。**

### 3.3 核心 JS 函数与数据对象（app.js）

**通用 / 基础设施**

| 名称 | 作用 |
|---|---|
| `$` / `$$` / `el()` | DOM 查询与建节点小工具 |
| `renderMd(bub, text)` | 气泡 Markdown 渲染（marked 优先，失败纯文本），并追加法条卡片 |
| `showPage(id)` | 页面切换（含进入 AI 页时回到问题列表）；合同页 DeepSeek 状态行由导航点击的独立监听 + 初始化时调用 `updateReviewStatus()` 刷新 |
| `toast(msg, ok)` | 全局轻提示 |
| `ensureLib(src)` | 按需动态加载 CDN 脚本（带缓存，失败抛错） |

**法条库与法律依据卡片**

| 名称 | 作用 |
|---|---|
| `LAW_DB` | 法条原文库：民法典 / 劳动合同法 / 消费者权益保护法 / 治安管理处罚法 / 劳动法共 30 条（key 形如 `民法典-709`） |
| `extractLaws(text)` | 从回答文本正则匹配《法律名》第 x 条引用，返回命中的 LAW_DB key 列表 |
| `appendLawCard(bub, text)` | 在气泡末尾渲染可折叠「📖 法律依据」卡片 |

**首页测评**

| 名称 | 作用 |
|---|---|
| `QUIZ_DIMS` / `QUIZ` | 5 大领域元数据 / 20 道题（题干、领域 dim、选项与风险分 p） |
| `quizReset()` / `renderQuizQ()` / `showQuizResult()` | 测评流程：重置 → 逐题渲染 → 得分率分级 + 分领域明细 |

**AI 助手**

| 名称 | 作用 |
|---|---|
| `SCEN` | 6 个场景的离线对话内容（`ask / ans / su` 追问） |
| `detectScene(text)` / `askAI(text)` | 关键词匹配场景 / 带问题跳 AI |
| `offlineAnswer(text)` / `demoAnswerFor(text)` / `fallbackAns()` | 离线 7 类兜底回答 / 离线取答唯一入口（场景优先，兜底其次）/ 通用建议 |
| `pushMsg()` / `botTyping()` / `botReply()` | 消息气泡 / 输入中动画 / 离线延时回复 |
| `FUZI_CFG` / `FUZI_DEFAULT` / `FUZI_SYS` | 模型配置（模式 / 地址 / 模型名 / Key）、默认值、「法小航」系统提示（变量名 FUZI 为历史命名，界面均显示 DeepSeek） |
| `loadFuziCfg()` / `saveFuziCfg()` / `modelOn()` / `fuziBase()` | 配置读写（localStorage 键 `deepseek_cfg_v1`）/ 是否模型模式 / 归一化接口地址（自动补 `/v1`） |
| `buildHeaders()` / `dsContent()` / `dsEmptyError()` | 鉴权头 / 内容提取（空 content 回退 reasoning_content）/ 空返回可读原因 |
| `askModel(text)` | AI 助手调 DeepSeek（12 轮历史、120s 超时、可中断、失败回退离线） |
| `openAiModal()` / `testFuziConnection()` / `updateAIModeUI()` | 设置弹窗 / 测试连接（`GET /v1/models`）/ 顶部模式状态刷新 |

**合同审查**

| 名称 | 作用 |
|---|---|
| `RISK` / `LOW_TIPS` | 离线演示报告：8 条高中风险 / 12 条低风险提示 |
| `SAMPLE_CONTRACT` | 示例租房合同全文（一键填入） |
| `ACCEPT` / `pickFiles()` / `setFile()` / `clearFile()` | 允许扩展名 / 文件选择入口（按类型分流解析）/ 文件芯片状态 |
| `extractPdf()` / `extractDocx()` / `extractImage()` | 浏览器本地抽取：pdf.js 文字层 / mammoth 纯文本 / Tesseract OCR（中英） |
| `clipText()` / `withTimeout()` | 文本截断（12,000 字）/ Promise 超时封装（解析 90s） |
| `enterScanStage()` / `setStep()` / `setBar()` | 扫描动画阶段与进度 |
| `dsOnline()` / `updateReviewStatus()` | 合同页判断 DeepSeek 是否可用 / 状态行渲染（含去配置按钮） |
| `callDeepSeekChat(messages, done, fail)` | DeepSeek 通用请求封装（JSON 模式、150s、空输出重试 1 次） |
| `analyzeWithDeepSeek(text)` | 合同审查：构造审查系统提示并调用模型 |
| `extractJsonObj()` / `parseContractReport()` | 从模型输出提取 JSON / 解析为报告对象（评分、计数、risks、low_tips） |
| `scoreColor()` / `verdictText()` / `setCounts()` / `animateScore()` | 评分颜色 / 签约建议 / 计数联动 / 圆环动画 |
| `renderRiskList()` / `renderLowTips()` / `showReportAI()` / `showReport()` | AI 报告风险卡 / 低风险块 / AI 报告入口 / 离线演示报告 |
| `startScan()` / `showFailStage()` | 扫描入口状态机（文本检查→DeepSeek 状态检查→审查→成功/失败）/ 失败页 |

**我的权益（本机档案）**

| 名称 | 作用 |
|---|---|
| `PROFILE_KEY` / `SESSION_KEY` / `rlHash()` | 档案 / 登录态 localStorage 键 / 密码简单哈希（djb2 变体，演示级非加密） |
| `rlGetProfile()` / `rlSaveProfile()` / `rlGetSession()` / `rlSetSession()` / `rlClearSession()` / `rlLoggedIn()` | 档案与登录态读写 |
| `rlApplyState()` / `rlSetMode()` / `rlFillProfile()` | 登录页与档案页切换 / 登录注册 Tab / 身份卡填充 |
| `EV_KEY` / `EV_MAX_FILE` | 证据材料 localStorage 键 `fahq_evidence_v1` / 单份 2MB 上限 |
| `evReadAll()` / `evWriteAll()` / `evFileToDataUrl()` / `evUsedBytes()` | 证据增删改查 / 文件转 dataURL / 已用容量 |
| `renderEvList()` / `showEvForm()` / `evOpenRec()` / `evAskDelete()` | 证据列表 / 保存表单（分类 + 备注）/ 查看 / 删除 |
| `CHAIN_KEYS` / `evChainData()` / `evChainRender()` / `evChainText()` / `evChainDownload()` | 6 类关键材料定义 / 证据链统计（覆盖、完整度、时间跨度）/ 渲染时间线与叙述 / 导出文本 / 下载 TXT |

**风险地图**

| 名称 | 作用 |
|---|---|
| `MAP_DATA` / `TYPE_META` | 6 个片区数据源（坐标、环比 trend、四类 counts、脱敏案例、AI 提问）/ 类型元数据 |
| `mapState` | 交互状态（类型筛选 / 热力开关 / 选中片区） |
| `renderStatsAndBar()` / `renderHeat()` / `renderPins()` / `renderOverview()` / `renderPinDetail()` | 指标条与占比条 / 热力层 / 热点 / 总览面板 / 片区钻取详情 |
| `refreshData()` / `renderMapAll()` | 模拟刷新 / 整页重渲染入口 |

> 接真实数据的最小改动：**只替换 `MAP_DATA` / `RISK` / `SCEN` / `QUIZ` 等数据对象，逻辑代码基本不动**。
> 换 AI 供应商：接口为 OpenAI 兼容协议，改设置弹窗里的「服务地址 + 模型名称 + API Key」即可（AI 助手与合同审查同时生效），无需改代码。

### 3.4 本机存储（localStorage）一览

| 键 | 内容 | 写入位置 |
|---|---|---|
| `deepseek_cfg_v1` | DeepSeek 模式 / 地址 / 模型名 / API Key | AI 设置弹窗 |
| `fahq_profile_v1` | 法律档案（姓名、高校、学号、身份、密码哈希） | 我的权益 · 注册 |
| `fahq_session_v1` | 当前登录态（学号、登录时间） | 登录 / 退出 |
| `fahq_evidence_v1` | 证据材料（dataURL + 分类 + 备注 + 时间，单份 ≤2MB / 总量约 ≤4MB） | 我的权益 · 证据上传 |

### 3.5 CDN 按需加载库（加载失败均有降级，不阻断主流程）

| 库 | 版本 | 用途 | 加载时机 | 降级 |
|---|---|---|---|---|
| `marked` | latest（jsDelivr） | 聊天气泡 Markdown 渲染 | `<head>` 随页加载 | 气泡纯文本展示 |
| `pdf.js`（pdfjs-dist） | 3.11.174 | PDF 文字层抽取 | 上传 PDF 时 | 提示粘贴全文 |
| `mammoth` | 1.6.0 | .docx 纯文本抽取 | 上传 Word 时 | 提示另存 .docx / 粘贴 |
| `Tesseract.js` | 5.0.5 | 图片 OCR（chi_sim+eng） | 上传图片时 | 提示粘贴全文 |

---

## 4. 设计规范（保持年轻 + 专业 + 可信）

- **配色**：蓝灰科技风。深蓝 `#0d2557` → 亮蓝 `#2563eb` → 青 `#0ea5e9` 渐变主视觉；背景浅灰蓝 + 细网格 + 光晕；白色卡片。
- **风险语义色**：红=高风险 / 黄=中风险 / 绿=低风险，全站统一（测评分级、合同评分、地图热点、风险标签共用）。
- **字体**：系统字体栈（PingFang SC / 微软雅黑），无外部字体依赖（可离线）。
- **形状**：圆角卡片（16–22px）、大阴影、桌面毛玻璃顶栏 / 移动端底部 Tab、专业克制动效。
- **图标**：emoji（🏠💼📄🗺️👤）降低实现成本、贴合年轻调性；答辩如需更商务可换 SVG。
- **新增组件约定**：法条卡片（`.law-card`）、失败页（`.review-fail`）、证据链（`.chain-*`）、登录（`.rl-*`）等样式均已在 styles.css 中按既有令牌实现，新增同类组件请复用变量与圆角 / 阴影规范。
- **不要**：政务蓝白红、国徽印章、密集表格政府风；内容可严肃，视觉要现代。

---

## 5. 免责与合规声明（务必保留）

- 离线模式内容与风险地图、离线演示合同报告等页面数据均为**演示 / 模拟**；DeepSeek 模式回答与 AI 合同审查报告由大模型生成，可能存在错漏（含错误条号），**均不构成法律意见**。
- 各页页脚 / 说明处已内置「不构成法律意见，紧急拨打 12348」提示，正式发布前请由法律专业人士复核并调整文案；`LAW_DB` 条文为学习摘录，适用请以官方最新公布文本为准。
- API Key 仅保存在使用者浏览器 localStorage 中，代码不内置任何 Key；开启云端模式后，**提问文本与合同文本**会发送至 DeepSeek 官方服务器，合同文件本身只在浏览器本地解析、不上传；请勿输入敏感个人信息。
- 「我的权益」档案与证据材料仅保存在本机浏览器：密码为简单哈希、非加密存储，证据存 localStorage 有容量限制；请勿在公共设备使用，正式产品需加密存储、实名认证与隐私政策。
- 若后续接入真实用户数据，需补充隐私政策、数据脱敏与后端存储。

---

## 6. 多人协作约定（供团队参考）

1. **三个文件各司其职**：结构改 `index.html`、样式改 `styles.css`、逻辑与数据改 `app.js`；复杂数据可先集中在 app.js 顶部便于评审。
2. **引用路径**：外部文件一律用相对路径（`./styles.css`、`./app.js`），文件名大小写与仓库完全一致（GitHub Pages 为 Linux 环境，大小写敏感）；发布后若更新静态文件，可加 `?v=日期` 版本号防缓存。
3. **加数据 ≠ 改逻辑**：新增片区 / 场景 / 风险条目 / 法条 / 题目只改对应数据对象（`MAP_DATA` / `SCEN` / `RISK` / `LAW_DB` / `QUIZ`）；新增交互再动函数。
4. **命名**：JS 对象用大写（数据）与小驼峰（函数）区分；id / class 语义化并配中文注释。
5. **不要引入构建步骤 / 框架**：原型坚持原生三件套；新增能力优先用 CDN 按需加载（参考 `ensureLib()`），且必须有加载失败降级，保证双击即用、断网可走离线演示。
6. **API Key 不入库、不写死**：Key 只由使用者在设置弹窗填写并存于本地浏览器；提交前确认没有硬编码 Key。
7. **三条 AI 路径都要验证**：改 AI 相关逻辑后，分别验证「AI 助手离线演示」「AI 助手 DeepSeek（含断网 / 失败回退）」「合同审查 DeepSeek（含未配置失败页 / 离线演示报告）」。
8. **提交前自检**：改完 JS 做语法检查（见第 7 节），并跑一遍 5 页演示动线；改样式后检查窄屏（手机宽度）布局。
9. **分工建议**：一人管数据内容（法条 / 案例 / 文案 / `LAW_DB`），一人管交互与视觉，一人管答辩 PPT；以本文档 + 代码注释为唯一事实源。

---

## 7. 快速自检清单

- [ ] 桌面顶部 7 项、移动端底部 Tab 5 项与「全部功能」弹层均能切换且无报错；`index.html` 与 `styles.css` / `app.js` 同目录、引用为相对路径
- [ ] 首页：搜索可跳 AI；20 题测评可出结果，分领域风险 chips 正常
- [ ] AI（离线）：6 场景入口 → 对话 → 追问 → 返回列表；自由输入关键词能命中分类兜底；回答中法条引用出现可折叠「📖 法律依据」卡片
- [ ] AI（设置）：⚙️ 弹窗可切换模式；不填 Key 测试连接应给出明确失败提示；保存后顶部状态点 / 标签正确变化
- [ ] AI（DeepSeek）：有效 Key → 测试连接绿色已连接 → 对话由模型回答（Markdown 正常渲染）；断网 / 错 Key 时错误气泡 + 离线回答且不卡死
- [ ] 合同审查（离线兜底）：未配置 DeepSeek 时点扫描进入失败页并提示原因；「🧪 查看离线演示报告」展示 72 分报告、筛选 / 展开 / 导出 TXT 正常
- [ ] 合同审查（文件解析）：TXT 直读；DOCX / PDF / 图片分别走 mammoth / pdf.js / OCR（首次需联网加载库）；.doc 与扫描件 PDF 给出明确提示；文本超 12,000 字截断提示
- [ ] 合同审查（DeepSeek）：粘贴示例合同 → 扫描动画 → JSON 报告渲染（评分 / 计数 / 风险卡 / 低风险块）；失败时失败页三个按钮可用
- [ ] 风险地图：筛选类型 → 点击热点出详情 → 热力开关 → 模拟刷新；指标总数与 MAP_DATA 求和一致
- [ ] 我的权益：注册 → 退出 → 登录流程正常；上传证据（含超 2MB 拦截）→ 分类保存 → 列表 / 删除；证据链生成时间线、覆盖度、缺失提示并可导出 TXT
- [ ] 无“杭州”残留（背景为昆明呈贡 / 云南大学）
- [ ] JS 语法检查通过（Node：抽取 `<script>` 引用的 app.js 执行 `new vm.Script(code)`）

---

## 8. 待办 / 路线图

### 8.1 「AI 咨询回复给出具体法律条文」——已落地 ✅

- ✅ **v2.0 已完成方案 A（离线可审计）**：内置法条库 `LAW_DB`（5 部法律 30 条原文），聊天气泡自动识别《法律名》第 x 条引用并渲染可折叠「📖 法律依据」卡片，离线 / 在线回答均生效；
- ✅ **方案 B（在线全覆盖）已在 v1.3 完成**：DeepSeek 云端模式系统提示要求引用具体法律名称与条号，v2.0 起合同审查报告同样要求逐条给出法律依据；
- 后续可选：扩充 `LAW_DB` 覆盖更多场景（消费、网络、知识产权条文原文）、接入法律垂直大模型 / RAG（法条语料库 + 引用溯源）与「引用校验门禁」防条号幻觉。

### 8.2 其他待办
- [x] ~~AI 接入真实大模型~~（v1.3：DeepSeek 云端 + 离线兜底双通道）
- [x] ~~法条库 `LAW_DB` + 聊天「法律依据」卡片~~（v2.0）
- [x] ~~合同审查接入真实 AI~~（v2.0：DeepSeek JSON 结构化报告 + 浏览器本地 PDF/Word/图片解析 + 失败页 / 离线报告兜底）
- [x] ~~证据材料本机存证~~（v2.0：真实上传 localStorage、分类备注、容量控制）
- [x] ~~证据链汇总 / 导出~~（v2.0：时间线 + 6 类覆盖检查 + TXT 导出）
- [x] ~~部署到 GitHub Pages~~（<https://xw205.github.io/student-legal-cockpit/>，相对路径 + 大小写注意）
- [x] ~~移动端底部 Tab 导航与「全部功能」弹层~~（UI v2.2：≤960px 去顶栏，改底部导航）
- [ ] 风险地图接入真实数据源（替换 `MAP_DATA`，或接后端接口）
- [ ] 合同审查支持多份文件 / 长合同分段审查；报告导出为 PDF / Word（当前为 TXT）
- [ ] 证据材料增加「到期提醒 / 时效倒计时」、容量升级（IndexedDB 替代 localStorage）
- [ ] 档案密码加密存储与多档案支持（当前单浏览器单档案、简单哈希）
- [ ] 风险测试题库扩充与结果分享卡片
- [ ] 精简版代码（降低二次开发阅读成本）

---

*文档结束 · 欢迎团队在 Issue / 群内基于本文档分工协作*
