# 法护启航 · UI 专业化改版设计说明（分支 ui/professional-redesign）

## 一、设计定位
面向「呈贡大学城大学生」的 AI 法律公益平台。整体采用 **现代简约政务/公益风**：稳重、克制、可信，不使用卡通化元素与花哨动画。

## 二、配色（蓝紫主色 · 公益稳重）
| 用途 | 色值 | 说明 |
| --- | --- | --- |
| 主品牌色 | `#4F46E5`（靛蓝紫） | 按钮、主链接、高亮 |
| 深主色 | `#3730A3` | 标题强调、渐变起始、底部深色块 |
| 品牌墨色 | `#1E1B4B` | 主文字/深底 |
| 辅助浅紫 | `#A5B4FC / #818CF8` | 渐变过渡、标签、图标底 |
| 中性灰 | `#46566F / #8A98AF` | 正文辅助、弱化信息 |
| 语义色 | `#10B981 绿 / #F59E0B 黄 / #EF4444 红` | 风险等级、状态（仅语义使用） |
- 渐变基调：`linear-gradient(135deg,#4338CA,#4F46E5,#818CF8)`，蓝紫过渡，避免高饱和花哨色。

## 三、字体与排版
- 字体栈：`PingFang SC / Hiragino Sans GB / Microsoft YaHei / Segoe UI / system-ui`（系统字体，保证离线与中英文观感）。
- 层级：页面大标题 `27px/800`，卡片标题 `17px/700`，正文 `13.5–14.5px`，注释 `11.5–12.5px`；行高 1.6–1.9。
- 版式：顶部 Logo+标语，内容卡片式网格；底部固定导航（桌面窄条 + 移动端圆角胶囊），响应式单栏。

## 四、页面结构与排版（7 页）
1. **首页**：Hero（项目名+口号）→ 搜索区 → 平台核心功能 7 宫格入口 → 风险科普卡片 → 备案说明。
2. **AI 风险测评**：问卷表单（20 题，进度条 `x/20`）→ 结果卡：风险等级 + 风险指数% + 五类分领域风险标签。
3. **AI 合同审查**：上传/粘贴区 + 扫描动效 → 审查报告（评分圆环 + 统计 + 可折叠风险卡片：原因/建议/法条）+ 失败提示界面。
4. **维权导航**：快捷问题列表 + 问答对话气泡（DeepSeek 在线/离线演示双通道）。
5. **证据急救**：证据清单（6 类勾选、完成度进度条）＋ 证据时间轴（来自我的法律档案，本地持久化）。
6. **线下服务地图**：标题区（呈贡大学城服务点导览）＋ 点位标记画布 + 详情侧栏（演示数据可替换）。
7. **关于我们**：项目简介、解决的问题、调研说明、技术架构、团队信息（响应式四卡）。

## 五、组件清单
卡片式布局（.card/.module-card/.about-card）、导航栏（7 项底部固定）、功能入口宫格、问卷/进度条组件、评分圆环（SVG）、数据统计条（.stat-mini）、地图点位（Canvas + 详情面板）、表单/上传区、时间轴（.tl-line/.tl-step）、清单勾选（.eg-item）、对话气泡与 chips、Toast 反馈。

## 六、交互逻辑标注（便于后续开发）
- 页面切换：`showPage(id)` 全局函数；底部 `.nav-item` 与首页 `.module-card` 均以 `data-page` 驱动。
- 测评：`quizReset/renderQuizQ/showQuizResult`；每题选项累计 `quizScore/quizDimScore`，按 `得分/60` 计算风险指数%，再分领域生成标签。
- 合同审查：`startScan()` 判断 DeepSeek 是否在线与文本来源 → `analyzeWithDeepSeek(text)` → `showReportAI`；失败走 `showFailStage`（不再自动展示模拟报告）。
- 维权导航：`openChat/askAI/askModel/botReply`；未启用模型自动离线兜底。
- 证据急救：`egRender()`（localStorage 记录勾选）+ `emRenderTimeAxis()`（读取 `evReadAll()` 生成时间轴）。
- 我的法律档案（从首页入口进入）：真实信息登录（`rlApplyState`）→ 证据上传（Base64→localStorage）→ 证据链生成（`evChainRender`）。
- 数据存储：统一 localStorage 键（测评勾选 `fahq_eg_v1`、档案 `fahq_profile_v1`、会话 `fahq_session_v1`、证据 `fahq_evidence_v1`）；AI 配置键 `deepseek_cfg_v1`。

## 七、适配与性能
- 响应式断点：`760px` 以下单列、导航压缩；桌面最大内容宽 `1080px`。
- 无框架、单文件、可直接双击运行；离线演示 + DeepSeek 在线双模式。